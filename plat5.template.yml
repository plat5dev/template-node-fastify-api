name: node-fastify-api
description: Node + Fastify + TypeBox reference API (profiles, projects, tasks)

upstreams:
  api: "3000"

routes:
  - ./routes.identity.yml
  - ./routes.yml

substitute:
  - package.json

next:
  - pnpm install
  - pnpm run dev
